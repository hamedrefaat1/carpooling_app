

import 'package:flutter/material.dart';

class HomeAppRider extends StatelessWidget {
  const HomeAppRider({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Home app rider!"),
      ),
    );
  }
}